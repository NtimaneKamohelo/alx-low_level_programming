#ifndef MAIN_H
#define MAIN_H

/**
 * File: mai.h
 * Desc: Header file containing prototypes for all functions used in the 0x06. C - More pointers, arrays and strings
 */

int _putchar(char c);
char *_strcat(char *dest, char *src);
